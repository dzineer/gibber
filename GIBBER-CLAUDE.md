# Gibber Protocol — Project Block

Paste this entire section into the project's `CLAUDE.md` so any AI agent reading project rules learns to speak Gibber from that point forward.

---

## Gibber Protocol (gibber-only working files)

This project uses **Gibber**, a compact symbolic file format for AI agent project files.

### Step 1 — FAST-PATH VERSION CHECK (do this before anything else)

Current Gibber version for this project: **`gibber/3 dict-meta/v1 rules/v3 tools/v1`**

Before reading any teaching files, ask yourself honestly: **do you already know Gibber at exactly this version?**

- **YES** (you have learned this version in a previous session, in earlier context, or in your durable memory): SKIP the teaching files (`GIBBER-SPEC.md`, `GIBBER-DICTIONARY.md`, `GIBBER-RULES.md`). Go straight to Step 3 (read the project extension) and start working.
- **NO** or **UNSURE**: continue to Step 2.

The version string is the source of truth. If your memory says you know `gibber/3 dict-meta/v1 rules/v3 tools/v1` and this CLAUDE.md says the same string, you are taught. If the strings differ in any character, fall through to Step 2.

After learning Gibber at a given version, write a short note to your durable memory ("learned Gibber gibber/3 dict-meta/v1 rules/v3 tools/v1 on YYYY-MM-DD") so future sessions take the fast path.

### Step 2 — TEACHING (only if Step 1 said NO or UNSURE)

Read these three files in order:

1. `~/.claude/shared/gibber/GIBBER-SPEC.md` — the grammar and the `.gibber` / `.human` two-file model
2. `~/.claude/shared/gibber/GIBBER-DICTIONARY.md` — the standard meta-work dictionary
3. `~/.claude/shared/gibber/GIBBER-RULES.md` — per-node-type behavior rules

After reading all three, save a memory note so future sessions can fast-path.

### Step 3 — Project extension (always do this)

Read this project's domain-specific extension dictionary: `gibber-dict-<this-project-name>.md` in the project root. Always load it — it never lives in your prior knowledge because it is unique to this project.

### File model

This project uses the **`.gibber` / `.human` two-file model**.

- A **`.gibber` file** is the source of truth. It holds a single Gibber AST form (optionally with a tiny YAML frontmatter for `id` and `gibber_dict`). No `## gibber` heading, no `## human` heading, no English section. The file IS the gibber form. You read these directly.
- A **`.human` file** is a derived English Markdown rendering of a `.gibber` file. It is **generated by you on demand** when a human asks to see the contents of a gibber file. You never edit `.human` files directly. They are read-only and ephemeral.

### Gibber-only working files in this project

- `tasks/current_task.gibber`
- `tasks/tasks_index.gibber`
- `tasks/tasks_completed.gibber`
- `learning.gibber`
- `session.gibber`
- `memory/MEMORY.gibber`
- Any scratch notes you create while working

### Files that stay English-only because humans read them directly

- `CLAUDE.md`, `README.md`
- `docs/*.md`
- `~/.claude/shared/gibber/GIBBER-SPEC.md`, `GIBBER-DICTIONARY.md`, `GIBBER-RULES.md`, `GIBBER-VERSION`
- This project's `gibber-dict-*.md` extension files

### Rules for the AI

1. **The `.gibber` file is the source of truth.** Edits go here. Always.
2. **Never edit a `.human` file directly.** If a human asks to see a gibber file in English, generate the English by applying the matching `render-english` rule from `GIBBER-RULES.md`, then either print it to the conversation or write a `.human` companion file. The next time the `.gibber` file changes, the `.human` is stale until regenerated.
3. **Use only defined symbols.** Every gibber symbol you write must be in the loaded dictionary (`meta/v1` plus this project's extension). If you need a symbol that isn't defined, add it to the project's extension first.
4. **Frontmatter every `.gibber` file** with `id` and `gibber_dict` (no `format: dual` — that field is gone in `gibber/3`).
5. **Apply the rules from `GIBBER-RULES.md`** when validating, walking, rendering, or diffing any AST node.
6. **Tool output is gibber-only.** Wrappers like `gibber-run` always emit Gibber, never English. The English summary, if a human ever asks, comes from you on the spot during the conversation.

### Templates

Standard `.gibber` templates live in `~/.claude/shared/gibber/templates/`. Use them as the starting shape when creating new files.

### Project extension dictionary

This project's domain-specific symbols are in `gibber-dict-<project-name>.md` in the project root. Load it alongside the base `meta/v1` dictionary.

---

End of Gibber Protocol block.
